# 📦 נוסח לאריזת פרויקט לתבנית לשיתוף

**למי זה מיועד:** מישהי שבנתה משהו (דשבורד, אוטומציה, כלי) ורוצה לארוז אותו לתבנית נקייה
שחברה אחרת תוכל לקחת ולהטמיע אצלה — בלי הפרטים והסודות האישיים.

**איך משתמשים:** פותחים Claude Code **בתוך תיקיית הפרויקט הקיים** ומדביקים את הבלוק שבין הקווים.

---

אני רוצה לארוז את הפרויקט הזה לתבנית נקייה לשיתוף, כדי שחברה תוכל לקחת ולהטמיע אצלה מאפס
בלי שום פרט אישי שלי. עשה את כל העבודה מקצה לקצה.

**חשוב — אל תיתקע:** עבוד ברצף עד הסוף. כשיש החלטה טכנית — קבל את ברירת המחדל הסבירה והמשך
(אל תעצור לשאול "להמשיך?"). עצור ותן לי לפעול **רק** בשלושת המצבים האלה:
(1) צריך התחברות לחשבון שלי בדפדפן, (2) צריך תשלום/אמצעי תשלום, (3) אתה לא בטוח אם ערך מסוים רגיש.
בכל שאר הדברים — החלט לבד והתקדם. בצע כך:

**1. הבן מה נבנה כאן**
- סרוק את הפרויקט והכן סיכום קצר: מה זה עושה, מאיזה חלקים הוא מורכב (שרת/דשבורד/DB/אינטגרציות),
  ואילו שירותים חיצוניים הוא צריך (Railway/Netlify/Vercel, מסד נתונים, Google, מייל, API-ים).

**2. צור עותק תבנית נפרד**
- צור תיקייה חדשה נפרדת (למשל `<project>-template`) והעתק אליה את קוד המקור בלבד —
  **בלי** `node_modules`, `.git`, קבצי סביבה, אישורים, או קבצי נתונים/session.

**3. נקה כל פרט אישי וסוד — קריטי**
- עבור על כל הקוד וחלץ החוצה למשתני סביבה כל: מפתחות API, טוקנים, סיסמאות, connection strings,
  כתובות מייל, מספרי טלפון, מזהי קבוצות/גיליונות/פרויקטים, כתובות דומיין אישיות, ושמות פרטיים.
- החלף ערכים אישיים מקודדים-קשיח בקריאה מ-`process.env.XXX` (או המקבילה בשפה), עם ברירת מחדל ריקה.
- הרץ חיפוש אחרון לווידוא ניקיון (מיילים, טלפונים בפורמט 972/05, מפתחות ארוכים, שמות אישיים) —
  ואל תשאיר שום דבר אמיתי בקוד.
- אם יש נתונים לדוגמה (רשומות אמיתיות, לקוחות) — החלף בדוגמאות גנריות או רוקן.

**4. הוסף קבצי הקמה**
- `.gitignore` שמתעלם מ-node_modules, קבצי סביבה, אישורים וקבצי נתונים.
- `.env.example` עם **כל** משתני הסביבה הנדרשים, הערות מה כל אחד, ומה חובה מול אופציונלי.
- `README.md` בעברית: מה הכלי עושה, דרישות, והוראות הקמה.
- אם הפרויקט כולל דשבורד/ממשק — צלם או תאר במילים איך הוא נראה ומה הוא מציג.

**5. מצה ותעד את כל התקלות והתיקונים — אל תשמיט כלום**
- חפור בכל המקורות כדי לאתר כל באג, מכשול ותקלה שהיו במהלך הבנייה, ואיך נפתרו:
  - היסטוריית הגיט המלאה (`git log --oneline` וגם תוכן הקומיטים — במיוחד כאלה עם "fix", "bug", "תיקון").
  - הערות בקוד שמסבירות "למה" (workarounds, "נפתר ב-", אזהרות, guard-ים).
  - קבצי README/הערות/תיעוד קיימים.
- **ודא שכל תיקון באמת אפוי בקוד התבנית ולא רק מתועד** — עבור אחד-אחד ובדוק שהתיקון קיים בקוד.
  אם תיקון היה תלוי-סביבה (הגדרה חיצונית, מבנה שירות) — תעד אותו במפורש בהוראות ההקמה כשלב חובה.
- הוסף ל-README קטע "⚠️ תקלות נפוצות — כבר פתורות" שמפרט לכל תקלה: **מה קרה → למה → איך נפתר → איך להימנע**.
  כלול גם מכשולי-הקמה (הגדרות שירות, מסד נתונים, הרשאות, גרסאות) — לא רק באגים בקוד.
- המטרה: מי שמטמיעה מקבלת מערכת שעובדת **מהפעם הראשונה**, בלי לחזור על אף תקלה שכבר נפתרה.

**6. כתוב נוסח התקנה מוכן (SETUP_PROMPT.md)**
- כתוב נוסח בעברית שמישהי אחרת תוכל להדביק ב-Claude Code שלה, והוא יקים לה את הכל מאפס:
  שכפול מ-GitHub, התקנת תלויות, פריסה לשירות הרלוונטי, הקמת מסד נתונים אם צריך, והגדרת משתני הסביבה.
- הנוסח צריך: (א) לפתוח בהסבר מה יידרש ממנה עצמה (התחברות לחשבונות, תשלום אם יש, חיבורים ידניים);
  (ב) לאסוף ממנה מראש את הפרטים להתאמה אישית; (ג) לעצור רק בנקודות שחייבות אותה.

**7. אימות סופי — ודא שהכל עובר נקי (אל תדלג!)**
- **סריקת סודות:** הרץ חיפוש על כל הקוד אחרי דפוסים רגישים (מיילים, טלפונים 972/05, מפתחות/טוקנים ארוכים,
  connection strings, "password", "secret", "key", שמות פרטיים). כל תוצאה אמיתית — הוצא למשתנה סביבה.
- **שלמות תלויות:** ודא שכל התלויות מופיעות ב-package.json (או המקביל), ושאין import לחבילה חסרה.
- **בדיקת הרצה נקייה:** שכפל את התבנית לתיקייה זמנית חדשה, הרץ התקנת תלויות, ונסה להעלות את האפליקציה
  עם `.env` ריק/דמה. ודא שהיא **לא קורסת בגלל ערך אישי חסר** ושהיא נטענת (גם אם לא מחוברת לשירותים) —
  זה מוכיח שכל הפרטים האישיים באמת יצאו החוצה ושום דבר לא חסר.
- **בדיקת שלמות מול המקור:** השווה את רשימת הקבצים והפיצ'רים בתבנית מול הפרויקט המקורי,
  וודא שלא נשמט אף חלק פונקציונלי. אם משהו חסר — הוסף אותו.
- **מעבר על התקלות:** עבור על קטע "תקלות נפוצות" ואמת שכל תיקון קיים בקוד או מתועד כשלב הקמה חובה.

**8. פרסם ל-GitHub — ציבורי, לצפייה מכל מקום**
- ודא שאני מחוברת (`gh auth status`; אם לא — `gh auth login` והדרך אותי).
- צור repo חדש בשם `<project>-template` ודחוף אליו.
- **פרסם אותו כ-Public כברירת מחדל** — כדי שהקישור ייפתח אצל כל אחת מכל מקום בלי הוספת הרשאות ידנית.
  (הרי כבר ניקינו כל סוד ופרט אישי, אז ציבורי בטוח.) אל תעצור לשאול על זה — אם אני *לא* רוצה ציבורי,
  אני אעצור אותך; אחרת המשך ישר לציבורי.
- ודא שהקישור באמת נגיש לצפייה: פתח את כתובת ה-repo ואת הקישור הישיר לקובץ SETUP_PROMPT.md
  (בפורמט `https://github.com/<user>/<repo>/blob/<branch>/SETUP_PROMPT.md`) וּודא שנטענים.
- אם לפרויקט יש דשבורד/אתר מתארח — ודא שההוראות ב-SETUP_PROMPT מפיקות **כתובת ציבורית לצפייה מכל מקום**
  (Generate Domain / Public URL בשירות הרלוונטי), ולא כתובת מקומית בלבד.
- בסיום תן לי מוכן-להעתקה: קישור ה-repo + הקישור הישיר ל-SETUP_PROMPT.md, כדי שאשלח מיד.

**9. הפק הודעת שיתוף מוכנה לחברות**
- כתוב לי בסוף **הודעה מסודרת אחת, מוכנה להעתקה** (בתוך בלוק קוד), בעברית וידידותית, שאוכל לשלוח כמו שהיא בוואטסאפ.
- ההודעה תכלול: (א) בקצרה מה הכלי עושה ואיזו בעיה הוא פותר; (ב) מה מקבלים (הפיצ'רים/הדשבורד העיקריים);
  (ג) הוראות התקנה פשוטות ב-3 שלבים — לפתוח את הקישור הישיר ל-SETUP_PROMPT.md, להעתיק את הנוסח, ולהדביק ב-Claude Code;
  (ד) מה יידרש מהמתקינה עצמה (התחברות לחשבונות, תשלום אם יש, חיבורים/QR אם יש); (ה) הקישור הישיר בפועל.
- שמור על טון קליל, אימוג'ים במידה, וקצר מספיק לשליחה בהודעה אחת.

חשוב מאוד:
- העבר לתבנית את **כל** מה שנבנה — כל הפיצ'רים, הדשבורדים, האוטומציות והתיקונים — שום דבר לא נחתך או "מפושט".
  ההבדל היחיד מהמקור צריך להיות: אפס פרטים אישיים/סודות. הפונקציונליות זהה ב-100%.
- אל תדחוף שום סוד או פרט אישי ל-GitHub. אם אתה לא בטוח אם ערך מסוים רגיש — שאל אותי.

בסיום תן לי סיכום: אילו פיצ'רים נכללו, רשימת כל התקלות שתועדו ונפתרו, אילו משתני סביבה צריך למלא,
ומה בדיוק נשאר למי שמטמיעה לעשות בעצמה.

---
